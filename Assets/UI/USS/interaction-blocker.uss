.interaction-blocker-main {
    width: 100%;
    height: 100%;
    align-items: center;
    justify-content: center;
}

.interaction-blocker-label {
    font-size: 140px;
    -unity-font-style: italic;
    opacity: 0.5;
}
